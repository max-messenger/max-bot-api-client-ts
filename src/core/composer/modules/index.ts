export {
  branch,
  catchMiddleware,
  compose,
  concat,
  drop,
  flatten,
  fork,
  lazy,
  optional,
  pass,
  passThrough,
  tap,
} from './composition';
export type { AsyncPredicate, Predicate } from './composition';
export { dispatch } from './dispatch';
export type { DispatchResult } from './dispatch';
